apply from: '../module.gradle'

android {
    defaultConfig {
        if (rootProject.ext.isApp) {
            applicationId "com.zql.splash"
        }
    }

}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    api project(path: ':lib_comm')
}
